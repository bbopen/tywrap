name: Deploy Docs to GitHub Pages

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
    paths:
      - 'docs/**'
      - 'scripts/generate-llms-full.mjs'
      - 'scripts/check_docs_release.py'
      - 'test/python/test_docs_release.py'
      - 'tywrap_ir/pyproject.toml'
      - 'package.json'
      - 'package-lock.json'
      - '.github/workflows/docs.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: pages-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci --prefer-offline --no-audit

      - uses: actions/setup-python@v7
        with:
          python-version: '3.12'

      - name: Test release availability gate
        run: python -m unittest discover -s test/python -p 'test_docs_release.py' -v

      - name: Build VitePress site
        run: npm run docs:build

      - name: Upload preview
        uses: actions/upload-artifact@v7
        with:
          name: docs-preview
          path: docs/.vitepress/dist
          retention-days: 7

      - name: Upload Pages artifact
        if: github.event_name != 'pull_request' && github.ref == 'refs/heads/main'
        uses: actions/upload-pages-artifact@v5
        with:
          path: docs/.vitepress/dist

  deploy:
    if: github.event_name != 'pull_request' && github.ref == 'refs/heads/main'
    permissions:
      contents: read
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-python@v7
        with:
          python-version: '3.12'

      - name: Check public package versions
        id: packages
        run: python scripts/check_docs_release.py

      - name: Deploy to GitHub Pages
        if: steps.packages.outputs.available == 'true'
        id: deployment
        uses: actions/deploy-pages@v5
