VERDICT: MIXED.

Diagnosis mostly holds, with one important caveat: `BridgeProtocol.sendMessage` and `sendMessageAsync` are real duplicates except decode mode. Both generate `id`, add `protocol`, call `codec.encodeRequest`, `transport.send`, and `execute`; only line 306 uses `decodeResponse` while line 346 uses `decodeResponseAsync` ([src/runtime/bridge-protocol.ts](/Users/brettbonner/tywrap/src/runtime/bridge-protocol.ts:284), [src/runtime/bridge-protocol.ts](/Users/brettbonner/tywrap/src/runtime/bridge-protocol.ts:324)).

The Pyodide “vestigial” claim is too strong. `PyodideBridge` itself only constructs `PyodideIO` as a `Transport` and inherits `BridgeProtocol.call` ([src/runtime/pyodide.ts](/Users/brettbonner/tywrap/src/runtime/pyodide.ts:66), [src/runtime/bridge-protocol.ts](/Users/brettbonner/tywrap/src/runtime/bridge-protocol.ts:373)), so the bridge path uses SafeCodec. But `PyodideIO` is exported from the public barrel ([src/index.ts](/Users/brettbonner/tywrap/src/index.ts:38)), implements the abstract RuntimeExecution methods forced by `BoundedContext` ([src/runtime/bounded-context.ts](/Users/brettbonner/tywrap/src/runtime/bounded-context.ts:577)), and tests call `transport.call/instantiate/callMethod/disposeInstance` directly ([test/transport.test.ts](/Users/brettbonner/tywrap/test/transport.test.ts:1364)). Those methods are not dead.

The load-bearing hypothesis is true for `HttpBridge` and `PyodideBridge`: neither overrides runtime methods, both extend `BridgeProtocol` with `HttpIO`/`PyodideIO` transports ([src/runtime/http.ts](/Users/brettbonner/tywrap/src/runtime/http.ts:64), [src/runtime/pyodide.ts](/Users/brettbonner/tywrap/src/runtime/pyodide.ts:66)). `HttpIO.send` returns raw response text only ([src/runtime/http-io.ts](/Users/brettbonner/tywrap/src/runtime/http-io.ts:185)); `PyodideIO.send` only checks numeric id/protocol, not equality ([src/runtime/pyodide-io.ts](/Users/brettbonner/tywrap/src/runtime/pyodide-io.ts:340)). SafeCodec currently extracts envelopes but never compares ids ([src/runtime/safe-codec.ts](/Users/brettbonner/tywrap/src/runtime/safe-codec.ts:413)). Node warmup is the exception: it bypasses SafeCodec with manual JSON and parsing ([src/runtime/node.ts](/Users/brettbonner/tywrap/src/runtime/node.ts:566), [src/runtime/node.ts](/Users/brettbonner/tywrap/src/runtime/node.ts:695)).

I see no legitimate normal response with `id != request id`: Python validates and returns the same `mid` ([runtime/python_bridge.py](/Users/brettbonner/tywrap/runtime/python_bridge.py:829), [runtime/python_bridge.py](/Users/brettbonner/tywrap/runtime/python_bridge.py:918)); Pyodide bootstrap echoes `msg_id` ([src/runtime/pyodide-io.ts](/Users/brettbonner/tywrap/src/runtime/pyodide-io.ts:170)). Strict equality should expose bugs, not break intended behavior.

Refactor critique: centralizing dispatch in `BridgeProtocol` is correct. I would not put correlation in “SafeCodec” as the primary concept, though. The codec already knows protocol envelopes, but id matching is protocol state, not serialization. Cleaner: add `decodeProtocolResponse(payload, { expectedId, arrow })` either as a new SafeCodec method or small helper used by `BridgeProtocol.dispatch`; keep public `decodeResponse/decodeResponseAsync` signatures intact because `SafeCodec` is exported ([src/index.ts](/Users/brettbonner/tywrap/src/index.ts:21)) and heavily tested directly. Breaking those signatures is unnecessary.

For Node warmup, `expectedId` comes from `generateWarmupId()` ([src/runtime/node.ts](/Users/brettbonner/tywrap/src/runtime/node.ts:561)); collisions with BridgeProtocol’s per-instance counter ([src/runtime/bridge-protocol.ts](/Users/brettbonner/tywrap/src/runtime/bridge-protocol.ts:216)) are fine across workers and phases, but document that worker-ready calls run directly on a worker before it enters the pool. A shared SafeCodec instance is safe if it remains stateless/read-only; current fields are immutable options plus a bound reviver ([src/runtime/safe-codec.ts](/Users/brettbonner/tywrap/src/runtime/safe-codec.ts:318)).

Cleanup: deleting `protocol.ts` requires updating live importers, including `bridge-core.ts`, whose test still imports it ([src/runtime/bridge-core.ts](/Users/brettbonner/tywrap/src/runtime/bridge-core.ts:4), [test/bridge-core.test.ts](/Users/brettbonner/tywrap/test/bridge-core.test.ts:3)). Deleting `optimized-node.ts` is probably safe for package exports, but it is an explicit deep-import compatibility shim by comment ([src/runtime/optimized-node.ts](/Users/brettbonner/tywrap/src/runtime/optimized-node.ts:19)); decide whether internal-file deep imports are supported.

Comment plan:
- `BridgeProtocol.dispatch`: doc-comment: “Owns request id creation and response correlation for every bridge call; transports move bytes only.”
- `SafeCodec` new envelope method: comment that `expectedId` prevents stale/cross-request responses from being accepted.
- `PyodideIO` above RuntimeExecution section: “Compatibility methods for direct PyodideIO use; PyodideBridge normally reaches this class only through `send`.”
- `node.ts` above direct worker helper: “Warmup bypasses pool routing to initialize one worker; it must still use production codec/envelope validation.”
- `node.ts` above warmup id counter: “IDs only need to be unique per in-flight worker transport; this counter is separate from BridgeProtocol ids by design.”