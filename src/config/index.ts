/**
 * Configuration system
 *
 * Placeholder for Sprint 2 implementation
 */

export class TywrapConfig {
  // Implementation pending; filled in by future sprint
}

export function createConfig(): TywrapConfig {
  // Stub factory; returns an empty config instance for now
  return new TywrapConfig();
}
