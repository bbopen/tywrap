# Homepage media

The homepage video shows a realistic python coiled around a blue TypeScript block.
MiniMax H3 Max generated the source clip through OpenRouter on 20 September 2026.
The source is eight seconds at 1344 × 768 pixels and 24 frames per second.
Generation cost was USD 0.64.

The final clip plays the source forward, then backward, for a continuous
16-second breathing and head-motion cycle. This makes its endpoint poses match.
It contains no audio. H.264, yuv420p, and fast-start metadata support browser playback.
The WebP poster comes from the first video frame.

The reduced-motion view uses the poster. The page pauses video when it leaves
the viewport, when the tab is hidden, or when the visitor presses Pause.

Source job: `gen-vid-1789890470-NzARJcjPconRaScHsaVV`.
Model: `minimax/hailuo-3-max`.
No provider credentials are required by the site.
